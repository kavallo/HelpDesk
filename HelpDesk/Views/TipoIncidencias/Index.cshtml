
@model IEnumerable<HelpDesk.Models.TipoIncidencia>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>
    Mantenimiento de Incidencias
</h2>

<p>
    @Html.ActionLink("Crear Nueva Incidencias", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Incidencia
            @*@Html.DisplayNameFor(model => model.Descripcion)*@
        </th>
        <th>
            Estatus
            @*@Html.DisplayNameFor(model => model.Estatu.Descripcion)*@
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estatu.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.TipoIncidenciaID }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.TipoIncidenciaID })
        </td>
    </tr>
}

</table>
