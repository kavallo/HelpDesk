@model IEnumerable<HelpDesk.Models.Departamento>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Departamentos</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Departamento
            @*@Html.DisplayNameFor(model => model.Descripcion)*@
        </th>
        <th>
            Descripción
            @*@Html.DisplayNameFor(model => model.Estatu.Descripcion)*@
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion, "Descripcion")
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estatu.Descripcion, "Descripcion")
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.DepartamentoID }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.DepartamentoID }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.DepartamentoID })
        </td>
    </tr>
}

</table>
